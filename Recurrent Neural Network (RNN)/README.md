# Recurrent Neural Network (RNN)

This program implements a **Recurrent Neural Network (RNN)** for processing sequential data such as text or time-series data.

RNNs are designed to remember previous information in a sequence, allowing the model to learn patterns and dependencies in sequential datasets.

**Technologies Used:**

* Python
* TensorFlow / Keras
* NumPy
