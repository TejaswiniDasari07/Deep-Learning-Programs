# One Hot Encoding for Text Data

This program demonstrates **One Hot Encoding**, a technique used to convert categorical or text data into numerical format so that it can be used in machine learning and deep learning models.

The program encodes words or categories into binary vectors where only one position is marked as 1 and the remaining positions are 0.

**Technologies Used:**

* Python
* TensorFlow / Keras
* NumPy
