# Word Embeddings

This program demonstrates **Word Embeddings**, a technique used in Natural Language Processing (NLP) to represent words as dense numerical vectors.

The model learns relationships between words and converts them into meaningful vector representations that capture semantic similarities.

**Technologies Used:**

* Python
* TensorFlow / Keras
* NumPy
